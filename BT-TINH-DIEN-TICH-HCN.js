function dienTichHCN() {
    var a = parseInt(prompt('nhập vào 2 giá trị a', ''));
    var b = parseInt(prompt('nhập vào 2 giá trị b', ''));
    var c = a * b
    document.write('Diện tích HCN =' + ' ' + c)
}

